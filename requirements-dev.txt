# Dev / test dependencies (install with: pip install -r requirements-dev.txt)
# Pulls in all production dependencies.
-r requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.0
pytest-asyncio==0.24.0
httpx>=0.27.0          # needed by FastAPI TestClient

# ── Linting ───────────────────────────────────────────────────────────────────
ruff==0.6.0

# ── Notebook / visualization (optional, not required for CI tests) ────────────
# Install manually when working on notebooks:
#   pip install jupyter matplotlib seaborn
# jupyter==1.1.1
# ipykernel==6.29.5
# matplotlib==3.7.5
# seaborn==0.13.2

# ── Model training (separate requirements file) ──────────────────────────────
# tensorflow and tensorboard are ONLY needed for training runs, not for
# application tests.  Install via:
#   pip install -r training/requirements_training.txt
#
# They are NOT included here because:
#   - tensorflow==2.13.0 is ~600 MB and adds 10-15 min to CI install time
#   - It conflicts with torch already in requirements.txt on some platforms
#   - No tests in tests/ require tensorflow
